{{#if (includes lifecycleMethods "getDerivedStateFromProps")}}

  static getDerivedStateFromProps(props, state) {
    //Called before initial render or update re-render
    return {}; //return object will change the initial state
  }
{{/if}}
